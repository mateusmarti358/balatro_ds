# Set bit depth to 8 (256 colors)
-gB8

# Ensure the output is tiled (Required for DS Sprites)
-gt

# Exclude the map
-m!

# Set metatile size to 4x4 (8*8 = 64 pixels wide, 8*8 = 64 pixels tall)
-Mw8
-Mh8
